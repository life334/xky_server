<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xakcch.project.mapper.ProjReportFilterMapper">

    <resultMap type="ProjReportFilter" id="ProjReportFilterResult">
        <result property="id"           column="id"            />
        <result property="filterName"   column="filter_name"   />
        <result property="filterConfig" column="filter_config" />
        <result property="delFlag"      column="del_flag"      />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
    </resultMap>

    <select id="selectFilterList" parameterType="ProjReportFilter" resultMap="ProjReportFilterResult">
        select id, filter_name, filter_config::text as filter_config, del_flag,
               create_by, create_time, update_by, update_time
        from proj_report_filter
        where del_flag = '0'
        <if test="filterName != null and filterName != ''">
            and filter_name like concat('%', #{filterName}, '%')
        </if>
        order by id desc
    </select>

    <select id="selectFilterById" parameterType="Long" resultMap="ProjReportFilterResult">
        select id, filter_name, filter_config::text as filter_config, del_flag,
               create_by, create_time, update_by, update_time
        from proj_report_filter
        where id = #{id}
    </select>

    <insert id="insertFilter" parameterType="ProjReportFilter" useGeneratedKeys="true" keyProperty="id">
        insert into proj_report_filter (filter_name, filter_config, del_flag, create_by, create_time)
        values (#{filterName}, #{filterConfig}::jsonb, '0', #{createBy}, now())
    </insert>

    <update id="updateFilter" parameterType="ProjReportFilter">
        update proj_report_filter
        <set>
            <if test="filterName != null">filter_name = #{filterName},</if>
            <if test="filterConfig != null">filter_config = #{filterConfig}::jsonb,</if>
            update_by = #{updateBy}, update_time = now()
        </set>
        where id = #{id}
    </update>

    <update id="deleteFilterById" parameterType="Long">
        update proj_report_filter set del_flag = '2', update_time = now()
        where id = #{id}
    </update>
</mapper>
